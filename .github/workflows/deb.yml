name: Build DEB

on:
  push:
    tags: ['v*']
    branches: [main]
  workflow_dispatch:

concurrency:
  group: "${{ github.workflow }}-${{ github.ref }}"
  cancel-in-progress: false

jobs:
  deb:
    uses: xiboplayer/.github/.github/workflows/build-deb.yml@e8649f4861f729365d7f15c9f5715f5df64dc2c7  # main
    with:
      package-name: xiboplayer-kiosk
      deb-script: 'deb/build-deb.sh'
      distro-matrix: '["ubuntu:24.04", "debian:trixie"]'
      arch-matrix: '["amd64", "arm64"]'
      gpg-sign: true
      # Same reasoning as rpm.yml: this workflow also runs on pushes to
      # main, and an unconditional publish overwrites the released DEBs
      # with a rebuild whose checksums no longer match the Packages index.
      publish-to-repo: ${{ startsWith(github.ref, 'refs/tags/v') }}
    secrets: inherit
