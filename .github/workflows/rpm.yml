name: Build RPM

on:
  push:
    tags: ['v*']
    branches: [main]
  workflow_dispatch:

concurrency:
  group: "${{ github.workflow }}-${{ github.ref }}"
  cancel-in-progress: false

jobs:
  rpm:
    uses: xiboplayer/.github/.github/workflows/build-rpm.yml@0ec252bd2d68b35cc0072b8d07bb0cb9141d21bd  # main
    with:
      package-name: xiboplayer-kiosk
      rpm-spec: 'rpm/xiboplayer-kiosk.spec'
      fedora-matrix: '["43", "44"]'
      source-tarball: 'full'
      gpg-sign: true
      publish-to-repo: ${{ startsWith(github.ref, 'refs/tags/v') }}
    secrets: inherit
