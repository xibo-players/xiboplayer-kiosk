name: Build RPM

on:
  push:
    tags: ['v*']
    branches: [main]
  workflow_dispatch:

concurrency:
  group: "${{ github.workflow }}-${{ github.ref }}"
  cancel-in-progress: false

jobs:
  rpm:
    uses: xibo-players/.github/.github/workflows/build-rpm.yml@3dcfbc978c4b1be83b2eea590f3d591a7142cdd1  # main
    with:
      package-name: xiboplayer-kiosk
      rpm-spec: 'rpm/xiboplayer-kiosk.spec'
      fedora-matrix: '["43"]'
      source-tarball: 'full'
      gpg-sign: true
    secrets: inherit
